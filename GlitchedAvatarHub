local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/Revenant", true))()
local Flags = Library.Flags
Library.DefaultColor = Color3.fromRGB(138, 43, 226)

local Window = Library:Window({
   Text = "Glitched Avatar Hub"
})

Window:Button({
    Text = "Wyvern",
    Callback = function()
loadstring(game:HttpGet('https://raw.githubusercontent.com/ckw69/Wyborn/main/wyborn', true))()
    end
})

Window:Button({
    Text = "Copy Item ID",
    Callback = function()
      setclipboard(" 134810391419525 ")
       Library:Notification({
           Text = "ugc id copied to clipboard",
           Duration = 3
       })
    end
})

-- porra de credito rgbolas
local hue = 0
local rgbLabel = Window:Label({
    Text = "YT: 14kGreenz",
    Color = Color3.fromHSV(hue, 1, 1)
})

task.spawn(function()
    while true do
        hue = (hue + 0.01) % 1
        rgbLabel:Set({
            Color = Color3.fromHSV(hue, 1, 1)
        })
        task.wait(0.05)
    end
end)

Window:Button({
   Text = "Copy Youtube link",
   Callback = function()
      setclipboard(" https://www.youtube.com/@14kGreenz ")
       Library:Notification({
           Text = "youtube link copied to clipboard",
           Duration = 6
       })
   end
})
